import React,{useEffect,useRef,useState} from 'react';
import {Download,Expand,Minus,Plus} from 'lucide-react';
import {mapLines,stationPositions} from './map-layout';
import {stationLines} from './metro';
export default function RouteMap({route,from,to,language='es'}){const [zoom,setZoom]=useState(1),viewport=useRef(null),svg=useRef(null),drag=useRef(null),active=new Set(route.path);useEffect(()=>{const el=viewport.current;if(!el)return;const p=route.path.map(s=>stationPositions[s]),x=p.reduce((n,a)=>n+a[0],0)/p.length*zoom,y=p.reduce((n,a)=>n+a[1],0)/p.length*zoom;el.scrollTo({left:Math.max(0,x-el.clientWidth/2),top:Math.max(0,y-el.clientHeight/2),behavior:'smooth'});},[route,zoom]);function download(){const copy=svg.current.cloneNode(true);copy.setAttribute('xmlns','http://www.w3.org/2000/svg');copy.setAttribute('width','1436');copy.setAttribute('height','1780');const svgUrl=URL.createObjectURL(new Blob([new XMLSerializer().serializeToString(copy)],{type:'image/svg+xml;charset=utf-8'})),image=new Image();image.onload=()=>{const canvas=document.createElement('canvas');canvas.width=1436;canvas.height=1780;canvas.getContext('2d').drawImage(image,0,0);URL.revokeObjectURL(svgUrl);canvas.toBlob(blob=>{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='mi-ruta-metro-cdmx.png';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0)},'image/png')};image.src=svgUrl}function down(e){const el=viewport.current;drag.current={x:e.clientX,y:e.clientY,left:el.scrollLeft,top:el.scrollTop};el.setPointerCapture(e.pointerId)}function move(e){if(!drag.current)return;const el=viewport.current;el.scrollLeft=drag.current.left-(e.clientX-drag.current.x);el.scrollTop=drag.current.top-(e.clientY-drag.current.y)}function up(){drag.current=null}return <div className="network-map"><div className="map-toolbar"><span>{language==='en'?'METRO NETWORK MAP · CDMX':'MAPA DE LA RED · CDMX'}</span><div><button className="map-download" type="button" onClick={download} title={language==='en'?'Download route as PNG':'Descargar ruta como PNG'}><Download size={17}/></button><button type="button" disabled={zoom<=1} onClick={()=>setZoom(z=>Math.max(1,z-.5))}><Minus size={17}/></button><button type="button" disabled={zoom>=4} onClick={()=>setZoom(z=>Math.min(4,z+.5))}><Plus size={17}/></button><button type="button" onClick={()=>setZoom(1)}><Expand size={17}/></button></div></div><div className="map-viewport" ref={viewport} onPointerDown={down} onPointerMove={move} onPointerUp={up} onPointerCancel={up}><svg ref={svg} style={{width:zoom*100+'%'}} viewBox="0 0 1436 1780" role="img" aria-label={'Ruta de '+from+' a '+to}><rect width="1436" height="1780" fill="#f8faf5"/>{mapLines.map(l=><polyline key={l.id} points={l.points.map(p=>p.join(',')).join(' ')} fill="none" stroke={l.color} opacity=".22" strokeWidth="9" strokeLinejoin="round" strokeLinecap="round"/>)}{route.segments.map((s,i)=><polyline key={i} points={s.stations.map(n=>stationPositions[n].join(',')).join(' ')} fill="none" stroke={s.line.color} strokeWidth="13" strokeLinejoin="round" strokeLinecap="round"/>)}{Object.entries(stationPositions).map(([name,[x,y]])=>{const selected=active.has(name),end=name===from||name===to,transfer=stationLines(name).length>1;return <g key={name}><title>{name+' · Línea '+stationLines(name).map(l=>l.id).join(', ')}</title>{end&&<circle cx={x} cy={y} r="18" fill="#ef5b24" opacity=".2"/>}<circle cx={x} cy={y} r={end?8:transfer?6:3.5} fill="white" stroke={selected?'#233d34':'#a5b2a1'} strokeWidth={selected?3:1}/>{selected&&<text x={x+12} y={y-9} fontSize={end?20:15} fill="#1d3429" fontWeight="700" paintOrder="stroke" stroke="#f8faf5" strokeWidth="4">{name}</text>}</g>})}</svg></div><div className="map-caption"><span>{language==='en'?'Route stations are labelled · drag to move':'Se muestran los nombres de tu ruta · arrastra para mover'}</span><button type="button" onClick={download}>{language==='en'?'Download route PNG':'Descargar ruta PNG'}</button></div></div>}
